<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Log In</title>
  <link rel="stylesheet" href="css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Jost:wght@500&display=swap" rel="stylesheet">
  <style>
    body {
      background-image: url('img/bg033.jpeg');
      background-size: cover;
      font-family: 'Jost', sans-serif;
      margin: 0;
      padding: 0;
      position: relative; /* Agregamos position:relative para que los elementos posicionados absolutamente se basen en este */
    }

    #logo {
      position: absolute;
      top: 30px;
      left: 80px;
      width: 300px; /* Ajusta el ancho según tu necesidad */
      height: auto; /* Para mantener la proporción de la imagen */
    }

    #logo a {
      text-decoration: none; /* Quitamos el subrayado del enlace */
      color: inherit; /* Hacemos que el color del texto sea el mismo que el color del texto circundante */
    }

    .forgot-password {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      color: white;
    }

    .forgot-password a {
      color: white;
      text-decoration: underline;
    }
  </style>
</head>

<body>

  <div class="overlay"></div>

  <div id="logo">
    <a href="/">
      <img src="img/logo.png" alt="Logo"> <!-- Agregamos la etiqueta img para el logo -->
    </a>
  </div>

  <div class="main">

    <input type="checkbox" id="chk" aria-hidden="true">

    <div class="signup">
      <form action="/register" method="post">
        <label for="chk" aria-hidden="true">Sign up</label>
        <input type="text" id="nameClient" name="nameClient" placeholder="Full Name" required>
        <input type="email" id="mailClient" name="mailClient" placeholder="Email" required>
        <input type="text" id="nationClient" name="nationClient" placeholder="Nationality" required>
        <input type="text" id="userClient" name="userClient" placeholder="Username" required>
        <input type="password" id="passClient" name="passClient" placeholder="Password" required>
        <button>Register</button>
      </form>
    </div>

    <div class="login">
      <form action="/login" method="post">
        <label for="chk" aria-hidden="true">Log in</label>
        <input type="text" id="userClient" name="userClient" placeholder="Username" required>
        <input type="password" id="passClient" name="passClient" placeholder="Password" required>
        <button type="submit">Log in</button>
      </form>
    </div>

    <div class="forgot-password">
      <a href="/resetpass">Forgot Password? Click here to reset password</a>
    </div>

  </div>
</body>

</html>
